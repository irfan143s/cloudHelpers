from enum import Enum

class CxAgentPlatforms(Enum):
        
    SOCIO = "Socio"
    CONNECT = "Connect"
    LIVE_PERSON = "LivePerson"

    # def __str__(self):
    #     return str(self.value)